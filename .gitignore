.DS_Store
.claude/settings.local.json

# ── marketing/ · NEVER COMMIT ─────────────────────────────────────────────
# Proposals, investor collateral, and PDF build tooling live here locally.
# vyb-web is served by GitHub Pages: everything committed to this branch is
# PUBLISHED at vybapp.io — even if the repo is later made private, Pages
# still serves every committed file. Confidential material stays untracked.
marketing/

# Stray PDF exports (the tracked site PDFs, e.g. VYB-sitemap.pdf, override this)
*.pdf

# Agent worktrees — local scratch, never part of the site
.claude/worktrees/
